# Spike-sorting curation agent：项目方法与实验状态

**状态日期：2026-09-23**

本文档只记录与论文方法、数据、实验设计和结果解释直接相关的内容。历史过程保留在 spike-sorting-agent_研究笔记.md；完整真实数据审计保留在 REAL_DATA_OPEN_VLM_STATUS_20260914.md。

## 1. 研究问题与范围

本项目研究的不是从原始电压信号开始取代完整 spike sorter，而是：

> 给定一次初始 spike sorting / overclustering 结果，能否让多模态 agent 像人类 curator 一样观察 waveform、ISI、amplitude、hierarchy 和数值质量指标，逐步执行 KEEP、SPLIT、DISCARD、MERGE、NOT_MERGE，最终得到更接近人工终态的 cluster assignments。

项目的主要方法定位是 **agentic post-curation**，而不是新的 spike detection 或底层 clustering 算法。

## 2. 三组容易混淆的流程名称

### 2.1 神经科学中的完整 spike-sorting 流程

1. **Recording**：电极记录细胞外电压。
2. **Preprocessing / detection**：滤波、去噪、检出 spike event。
3. **Feature extraction**：提取 waveform、PCA/template 等特征。
4. **Initial clustering / sorting**：使用 MountainSort、Kilosort 等得到初始 units/clusters。
5. **Post-curation**：人工或 agent 拆分混合 unit、合并重复 unit、删除噪声 unit。
6. **Quality control and evaluation**：计算 ISI、SNR、precision/recall、ARI 等。

本项目的 VLM agent 主要位于第 5 步；它使用第 3–4 步的产物，并在第 6 步被评估。

代码范围需要再区分：

- **真实数据主线**：`src/io/matlab_loader.py` 从已有 MAT 中读取 spike times、waveforms、overcluster/hierarchy assignments 和人工标注。它不从原始连续电压重新完成滤波、spike detection 和 initial sorting。
- **模拟分支**：`src/simulate/` 包含 MEArec 生成、SpikeInterface/MountainSort5 和 overclustering，能验证较完整的前置流程，但不是当前真实数据主线。
- **MATLAB 脚本**：`matlab_scripts/` 保留原有分群/人工 curation 工具；当前 Python agent 不会自动用它们将原始连续信号重跑成 MAT。

因此，当前论文方法的准确说法是 **post-curation agent**，而不是已完成的 raw-to-spikes 端到端新 sorter。

### 2.2 当前真实数据 curation pipeline 的 Phase

| Phase | 当前实现 | 真实数据默认状态 |
|---|---|---|
| Phase 0 | 按 spike 数的自动预过滤 | 关闭（threshold=0） |
| Phase 1 | VLM 对单个 cluster 做 KEEP / SPLIT / DISCARD | 启用 |
| Phase 2 | VLM 对 cluster pair 做 MERGE / NOT_MERGE / DISCARD | 启用 |
| Phase 3 | 按最终 spike 数的自动过滤 | 关闭（threshold=0） |

README 中的概念性 Phase 0 描述与当前 PureVLMCurationPipeline 代码存在历史术语差异。写论文时应以上表的当前实现为准，并明确报告 Phase 0/3 阈值。

### 2.3 模拟实验的 Stage 1–6

Stage 1–6 是模拟 benchmark 的实验组织方式：模拟、GT 动作、teacher-student trajectory、adaptation、alignment 和 sweep。它与真实数据 Phase 0–3 不是同一套编号。当前研究主线已转向真实数据，不继续扩展模拟 setting。

## 3. Agent 的方法定义

### 3.1 环境状态

这里的“环境”不是 Runpod 或 Python 环境，而是当前 recording/channel 的 spike 分组状态。第 t 步状态可表示为：

s_t = {assigns_t, hierarchy_t, active_clusters_t, spike_times, waveforms}

- assigns_t[i]：第 i 个 spike 当前属于哪个 cluster；0 通常表示已丢弃/噪声。
- hierarchy_t：初始 overclustering 和可用的拆分关系。
- active_clusters_t：当前仍存在的 cluster ID。
- spike_times 和 waveforms：原始 spike event 的时间和波形，在 curation 中只读。

### 3.2 VLM 观察

Pipeline 从当前状态生成观察：

- Phase 1：waveform overlay、ISI histogram、amplitude distribution、aggregation tree；
- Phase 2：small-cluster waveform、large-cluster waveform、merged ISI histogram；
- 数值特征：spike count、ISI violation rate、amplitude CV 等；
- 当前 phase 和允许的动作集合。

诊断图由 src/agent/runner.py 生成。当前 1,374 个真实人工动作对应 5,327 张 PNG，位于：

output/real_manifest_action_dataset_20260914/datasets/DATASET_ID/images/SAMPLE_ID/

### 3.3 VLM 的职责

Qwen/Gemma 在当前方法中是 **decision policy**，不是整个工程的自由 planner：

pi_theta(observation_t) -> action_t

它不负责选择数据集、决定运行哪个 phase、管理文件、修改 MAT 或计算评估指标。这些由确定性 Python controller 处理。“固定 controller + 可学习多模态 policy + 显式工具动作”仍属于 agentic workflow，而且比让 LLM 自由编排整个实验更容易审计。

### 3.4 状态转移

Controller 使用 ClusterManager 执行动作：

- SPLIT：沿已有 hierarchy 将一个 cluster 拆成子 clusters；
- MERGE：将两个 clusters 的 spikes 合并到同一 ID；
- DISCARD：将该 cluster 的 spikes 标为噪声/丢弃；
- KEEP / NOT_MERGE：保留当前 cluster 或保持两个 clusters 分离；
- ABSTAIN：模型输出无法解析或无法安全决策，不修改 assignments。

动作把 s_t 转换成 s_(t+1)。下一步看到的图片、指标和 active clusters 因此会改变，这就是“更新 cluster/环境状态”。

## 4. 真实数据、标注与当前用法

### 4.1 数据规模

- 17 个 current-format MAT，共 4,469,063 个 spikes；
- 16 个有可用人工终态和动作源；
- cM2-e004_001-003_CH5 没有监督标注，不进入定量评测；
- 1,374 条可回放专家编辑动作：SPLIT 636 / DISCARD 569 / MERGE 169；
- 241 条有人工 rationale，1,133 条只有 action label；
- 5,327 张重生成的 VLM 诊断图，约 486 MiB。

### 4.2 默认无泄漏划分

| Split | Recording blocks | 动作数 | 用途 |
|---|---|---:|---|
| Train | cM2-e004_001-003, cM2-e004_011-015, cM2-e007_012-017 | 973 | action-only SFT |
| Validation | cM2-e008_021-028 | 90 | CH30 base/SFT 模型选择 |
| Final test | cM2-e004_004-006 | 311 | 最终一次评测 |

Recording block 是根据路径/文件名推定，尚未获得数据提供者的 session metadata 确认。论文中必须报告这一限制，并在可能时做分组敏感性分析。

### 4.3 标注版本问题的处理

| 数据问题 | 当前处理 | 剩余风险 |
|---|---|---|
| Tianmin 四个 MAT 与 Jacob 数据重复 | SHA-256 确认相同，不重复计数 | 无 |
| Excel/CSV 动作与 MAT 终态不总是同一 curation pass | 只有 CH3/CH31 标为 unified；其余 14 个标为 action/terminal separate | separate 数据不能称为统一 GT trajectory |
| CH20 旧 CSV 第 44 步引用已消失 cluster | 主路线使用可完整回放的 MAT internal log | 旧 CSV rationale 不作 canonical trajectory |
| CH5 无 action/终态 | 排除训练和定量测试 | 只用于无标注 inference 或人工补标 |
| oldFormat / Chronux sampledata 格式不同 | 不混入 current-format 主数据集 | 需单独 adapter 和报告才能加入 |
| 不同 recording 有同名 CH | 新路线使用完整 dataset ID | 旧 CH-only 结果只作历史证据 |
| 动作日志只记录真实编辑 | 保留 human provenance，不伪造 KEEP/NOT_MERGE | 完整 decision dataset 仍缺负例 |

可解决的工程和版本问题已通过去重、回放、hash、provenance 和 separate/unified 标记处理。KEEP/NOT_MERGE 缺失和 session identity 未确认仍是未解决的数据限制。

### 4.4 数据问题的影响、现方案与替代方案

| 问题 | 严重度 | 当前方案的优点 | 当前方案的代价 | 其他可行方案 |
|---|---|---|---|---|
| 动作日志与终态不是同一 curation pass | **高**：会使统一轨迹指标失真 | 分成 action-level 与 terminal cluster-level，不伪造一致性 | 14 个数据集不能声称“逐步全对且终态也对” | 请数据提供者给出 matching canonical files；或让专家重放/复核一个小型高质量子集 |
| CH20 旧 CSV 存在不可执行步骤 | **中**：影响旧 reasoning 轨迹，不影响 MAT internal actions | 改用可完整回放的 MAT log | 失去 CH20 的人工 rationale | 获得完整匹配 log；或仅对前 43 步做带 rationale 的辅助分析 |
| 只有正向编辑，无 KEEP/NOT_MERGE | **高**：自主 agent 可能过度编辑 | 不伪造 human label，现有 1,374 条仍可训练编辑识别 | 不能仅凭现有 action baseline 证明会正确停止或拒绝错误 merge | 由终态构造 derived KEEP；专家补标 pairwise NOT_MERGE；或用 API teacher 生成训练负例并单列 provenance |
| recording/session 只由路径推定 | **中到高**：若推定错，会造成训练/测试泄漏 | 比按 CH 随机分割更保守 | block 数只有 5，方差大且真实独立性未证实 | 数据提供者确认 animal/session/probe/time；在此前报告 leave-one-inferred-block-out 敏感性 |
| CH5 无标注 | **低**：对监督数量影响小 | 排除可避免把未知当真值 | 暂时不能计分 | 作为盲测/主动学习池，待模型稳定后请专家补标 |
| oldFormat / Chronux 接口不同 | **中**：限制外部泛化数据 | 不将不兼容 schema 强行混合 | 可用数据量减少 | 建单独 adapter，对每个字段和时间单位做对照测试后作 external-domain 评估 |
| 同名 CH 与重复 MAT | **已解决，原风险高** | 完整 dataset ID + SHA-256 使样本可追溯 | 路径更长，需维护 manifest | 无需更弱的替代方案 |

这些问题不会让数据“无法使用”，但会限制论文能声称什么。最重大的两项是 **action/terminal 版本不统一** 和 **缺少 KEEP/NOT_MERGE 负例**；前者影响真值解释，后者直接影响自主闭环的安全性。

## 5. DISCARD 的保留与安全语义

DISCARD 没有从 agent 动作空间删除。它非常重要：1,374 条人工编辑中有 569 条 DISCARD。

关闭的是两种未校准的自动数量规则：

- Phase 0：cluster 少于 500 spikes 就直接删除；
- Phase 3：最终 cluster 少于 5,000 spikes 就直接删除。

旧 GPT 运行会由这些阈值生成大量非模型 DISCARD，但尚未证明阈值适合所有真实 recording。新路线默认设为 0，以免把数量少但可能有效的 neuron 不可逆删除。

### 5.1 500/5,000 阈值的来源与实证检查

代码历史显示，这两个数字从项目初始提交就存在，注释为“数量太少无法独立建立可靠统计”和“最终有效 cluster 的目标是至少 5,000 spikes”。但仓库没有保留阈值校准实验、recording duration 归一化或文献依据，因此它们是 **lab-specific heuristic**，不是通用生理学定律。固定 spike count 还会与录制时长和 firing rate 耦合：相同 500 spikes 在短 recording 与长 recording 中代表完全不同的 unit 活动水平。

从已导出的 1,374 条人工动作看：

- 人工 DISCARD 的 569 个源 cluster 中，只有 95 个小于 500；说明大多数人工丢弃还依赖波形/ISI，不能由数量代替。
- 8 个小于 500 的 cluster 被人工 MERGE；若先自动丢弃，这些专家动作会被破坏。
- 人工最终保留的 39 个 units 中，3 个少于 5,000 spikes（约 4,063、4,162、4,582）；固定 5,000 会与这批人工真值直接冲突。
- 但 39 个人工最终 units 都不少于 500，说明 500 在这批数据上可能是一个有用的候选特征，但仍不应在 merge 之前作为硬删除规则。

外部工具也通常将质量门槛设为可配置且联合多指标，例如 ISI、SNR、presence ratio 和 firing rate，而非通用的 500/5,000 硬规则。SpikeInterface 明确支持基于多质量指标的阈值或学习式 curation；新近的 SpikeLab 甚至将默认 minimum spike count 设为 50，进一步说明这一数字依赖数据和任务，不能直接移植。更好的处理是：保留 spike count 作为模型特征；以人工标注在 train/validation 上校准阈值；对 `off / 500 / calibrated` 做 ablation；并将硬删除改为可回滚的低置信标记。

阈值设计参考：

- SpikeInterface model-based curation: https://spikeinterface.readthedocs.io/en/latest/tutorials/curation/plot_1_automated_curation.html
- SpikeLab configurable curation: https://www.biorxiv.org/content/10.64898/2026.04.25.720833v1.full
- SpikeInterface reproducible pipeline QC: https://elifesciences.org/articles/110170

当 VLM 明确、合法地输出 DISCARD 时，当前 pipeline 仍执行删除。只有以下失败情况改为保守处理：

- provider 失败：终止，不回退 mock；
- JSON/动作解析失败：ABSTAIN，保留 cluster；
- 没有合法 merge target：保留 cluster；
- 所有 pair 均为 NOT_MERGE：保留为独立 cluster。

这将“科学判断为噪声”与“软件/输出失败”分开，是安全且必要的。

## 6. 模型使用状态与历史结果

### 6.1 旧真实数据 VLM 闭环

真实 MAT 数据已经在旧路线中用过 VLM：

- output/main_gpt-4.1/：CH3/20/30/31，共 506 条 pipeline actions；
- output/main_gpt-5.1/：CH3/20/30/31，共 180 条 pipeline actions；
- output/ablation_no_metrics/：CH3/20/30/31，共 175 条 pipeline actions。

这些是真实闭环运行，产生了诊断图、action log、final assignments 和 cluster-level reports。它们证明方法概念可运行，但不作为新 student 路线的最终 baseline，因为使用了：

- CH-only 路径和较窄的四通道数据；
- 未校准的 500/5,000 自动 discard 阈值；
- 修复前的解析失败和 NOT_MERGE 语义；
- 与当前 SFT 不完全一致的观察格式；
- 项目自定义而非外部 benchmark 统一的 cluster matching 指标。

旧结果可作为 preliminary evidence 或重算后的 ablation，但不应与新 Qwen/Gemma 结果直接混表。

### 6.2 旧 GPT 结果究竟证明什么

它们证明的是 **feasibility**：真实 MAT 能被加载，诊断图能发给 API VLM，模型能返回结构化动作，controller 能执行 SPLIT/MERGE/DISCARD 并产生终态。它们还提供两个可供后续验证的初步现象：GPT-5.1 在旧内部指标上优于 GPT-4.1；移除数值 metrics 后平均 F1 下降。

它们不能证明 SOTA，因为没有公开 blind ground truth benchmark、新旧协议不同、只有四个 CH，且部分 DISCARD 来自规则或修复前的错误语义。当前 README 的 F1 只能称 **legacy internal result**，不是与现有 sorter 的已验证 SOTA 比较。

CH30 上 GPT-4.1 有 62 条 log，GPT-5.1 有 22 条，不是后者“少做了 40 个固定题”：

| CH30 旧闭环 | Phase 0 规则丢弃 | Phase 1 | Phase 2 | 终态 |
|---|---:|---|---|---|
| GPT-4.1 | 11 | 17 SPLIT / 11 KEEP / 13 DISCARD | 10 DISCARD | 1 unit，50,821 spikes |
| GPT-5.1 | 11 | 1 SPLIT / 3 KEEP / 5 DISCARD | 2 DISCARD | 1 unit，50,821 spikes |

GPT-4.1 更倾向递归 SPLIT，因此创造了更多子状态和后续动作；GPT-5.1 更早将父 cluster 判为 DISCARD，轨迹因而较短。两者在 CH30 恰好收敛到同一个终态，但不能由此推广到其他数据。动作数不是越少越好：较短可能是高效，也可能是过早丢弃；必须看终态 recall、错误 DISCARD 和 cluster-level 指标。

### 6.3 当前安全真实数据路线

已完成：MAT 审计、动作回放、manifest、无泄漏 split、1,374 条样本和 5,327 张诊断图；另已在 Runpod H100 上完成 CH30 前 3 条 smoke、Qwen3.5-4B 全 90 条正式 v2 baseline，以及 Qwen3.5-2B/9B 和 Gemma-4-E4B-it 的同协议横向对照。

尚未完成：train-recording-only numeric baseline、图像/数值 ablation、可信 KEEP/NOT_MERGE 负例、SFT 和修复后 autonomous rollout。

因此，“当前多个开源 VLM 已按冻结协议跑通完整 CH30 固定状态评测”为真；但它们都没有可靠学会 DISCARD，且现有动作日志缺少 KEEP/NOT_MERGE ground truth，不能直接进入自主执行。

## 7. Base-model smoke、action-level baseline 与 autonomous rollout

### 7.1 CH30 3-sample smoke

所谓 3-step smoke 更准确地说是 **3-sample fixed-state integration smoke**：

1. 从 CH30 的 90 个人工动作状态中取前 3 个；
2. 向未微调 Qwen3.5-4B 发送每个状态的 prompt、图像和数值指标；
3. 检查模型是否返回合法 action JSON；
4. 记录显存、延迟、原始输出和与人工 action 的匹配。

只用 3 个样本是为了尽快暴露环境、图片顺序、vLLM、JSON schema 或模型兼容问题。它不是准确率实验，也不更新 cluster 状态。

2026-09-21 已在 Runpod H100 80 GB 上完成该 smoke：模型为未微调的 `Qwen/Qwen3.5-4B`，关闭 thinking，未调用外部模型 API、mock 或 RAG。三条样本的人工动作均为 `DISCARD`，预测依次为 `SPLIT / DISCARD / SPLIT`，即命中 1/3。由于只有 3 条且没有类别覆盖，这不能作为准确率结论；它证明了 4 张真实诊断图、数值特征、vLLM 推理、动作解析和结果归档链路可运行。

三条响应均能解析为合法 action，但在 `enforce_action_only=true` 且未启用 response schema 时，原始输出仍是带 rationale 的 JSON，而不是严格单动作输出。正式 baseline 应将“可解析有效率”和“严格格式遵从率”分开报告。当前错误还提示一个待验证假设：base model 可能过度依据 overcluster/ISI 值选择 `SPLIT`，而低估专家将该 cluster 判为 `DISCARD` 的图像证据。

已校验并下载的实验归档位于 `output/runpod_collected/qwen35_ch30_real_image_smoke_h100_20260921/`。

### 7.2 Action-level baseline

Smoke 通过后，在 CH30 全 90 个固定人工状态上评估 base model。CH30 有 DISCARD 41 / MERGE 6 / SPLIT 43。每个样本独立使用人工轨迹中的正确 pre-action state，因此属于 teacher-forced/fixed-state evaluation。

建议报告：

- overall action accuracy；
- per-action precision / recall / F1 和 macro-F1；
- confusion matrix；
- invalid-output rate 和 abstention rate；
- 单样本延迟与成本。

由于当前人工 log 没有显式 KEEP/NOT_MERGE，第一版应称为 **expert edit-action baseline**，不能称为完整六类 decision baseline。

2026-09-21 已在 H100 上完成首轮 90 条运行。输入为真实诊断图和数值特征，模型为未微调 `Qwen/Qwen3.5-4B`；未使用 API、mock 或 RAG，也未执行状态转移。

| 指标 | DISCARD | MERGE | SPLIT | 总体 |
|---|---:|---:|---:|---:|
| Support | 41 | 6 | 43 | 90 |
| Precision | 0.067 | 1.000 | 0.475 | — |
| Recall | 0.024 | 1.000 | 0.674 | — |
| F1 | 0.036 | 1.000 | 0.558 | macro 0.531 |
| Correct | 1 | 6 | 29 | 36 / 90（accuracy 0.400） |

预测分布为 `DISCARD 15 / MERGE 6 / SPLIT 61 / KEEP 5 / INVALID_ACTION 3`。混淆的核心是：41 个专家 DISCARD 中，32 个被预测为 SPLIT，5 个为 KEEP，3 个因输出截断而无效，只有 1 个正确；43 个专家 SPLIT 中，29 个正确、14 个被预测为 DISCARD；6 个 MERGE 全部正确，但样本太少，不能据此宣称稳定泛化。

87/90 输出可解析为完整 action JSON，3/90 为截断后的无效 JSON，ABSTAIN 为 0；90/90 都没有遵守 prompt 的“严格单 token”要求，而是输出 JSON+rationale。因此该运行是有价值的首轮 base-model diagnostic baseline，证明了强 SPLIT 偏置和 DISCARD 失败，但正式模型比较前应先冻结无冲突的 action-only schema 并复跑。归档位于 `output/runpod_collected/qwen35_ch30_real_image_full90_h100_20260921/`。

统一复核还发现：3-sample smoke 与全量前三条的 action/raw response 完全一致，因此不得重复计数；同一评估集上的 stage-majority 诊断参照为 49/90（54.4%），高于 Qwen 的 36/90。CH30 内事后选择的单一 `n_spikes` 阈值可分对 SPLIT/DISCARD 80/84；由于阈值选择和评估使用同一 validation recording，这不是可报告的 held-out baseline，但证明 numeric shortcut 风险很强。正式 numeric-only 模型必须只在 train recording 上拟合，再到 CH30 评估。完整分析见 `CH30_QWEN_BASELINE_ANALYSIS_20260921.md`。

首轮运行后已实现 `action-only-json-v2`：评估器先移除数据 prompt 末尾的 legacy rationale 要求，再追加唯一的 `{"action":"ACTION"}` 合约；action schema 使用 `additionalProperties=false`，正式评测默认开启 response schema。vLLM 0.29 路径改用标准 `response_format=json_schema`，schema 失败直接报错，不静默退回自由文本。代码和单元测试已完成，真实 vLLM 验证结果如下。

2026-09-21 已完成真实 vLLM 验证：3-sample smoke 和 CH30 全 90 条的严格 action-only JSON 合规率均为 100%，无截断或解析失败。正式 v2 结果为 accuracy `34/90 = 0.378`、macro-F1 `0.530`；DISCARD 仍只命中 1/41，说明输出协议问题已解决，但 base Qwen 的 curation 决策能力仍不合格。

### 7.2.1 同协议 open-VLM 横向对照

2026-09-21 又在同一 H100、同一 90 条 CH30 fixed-state 样本和同一 `action-only-json-v2` 协议下完成 Qwen3.5-2B、Qwen3.5-9B 与 Gemma-4-E4B-it。四个模型均为严格 JSON 90/90，结果可直接横向比较。

| 模型 | Accuracy | Macro-F1 | DISCARD recall | SPLIT recall | MERGE recall |
|---|---:|---:|---:|---:|---:|
| Qwen3.5-2B | 0.067 | 0.333 | 0 | 0 | 1.000 |
| Qwen3.5-4B | 0.378 | 0.530 | 0.024 | 0.628 | 1.000 |
| Qwen3.5-9B | 0.500 | 0.570 | 0 | 0.907 | 1.000 |
| Gemma-4-E4B-it | 0.544 | 0.578 | 0 | 1.000 | 1.000 |

Gemma 的 `49/90` 与同集 stage-majority 诊断参照准确率相同，并不表示两个预测序列完全相同。所有模型的主要问题仍是 DISCARD：4B 仅命中 1/41，其余为 0/41。MERGE 虽均为 6/6，但当前只有正例、没有 NOT_MERGE，不能作为完整 merge 能力结论。完整比较见 `CH30_OPEN_VLM_COMPARISON_20260921.md`。

### 7.2.2 Numeric-only supervised baseline

2026-09-21 已在本地 CPU 完成 leakage-aware numeric baseline。模型只使用三个 train recording blocks；Random Forest 与 HistGradientBoosting 的选择依据是 train-only leave-one-recording-block-out CV，CH30 和 final-test 均未参与选择。最终 RF 在 train grouped OOF 的 accuracy/macro-F1 为 `0.904/0.903`，因此在查看 CH30 前锁定为 primary。

CH30 上 RF 的 split-stage accuracy 为 `78/84 = 0.929`；加上单独报告的 6 个 positive-only MERGE 后，overall 为 `84/90 = 0.933`、macro-F1 `0.952`。DISCARD P/R/F1 为 `1.000/0.854/0.921`，SPLIT 为 `0.878/1.000/0.935`。6 个错误全部是 DISCARD→SPLIT，没有错误删除人工 SPLIT。HistGradientBoosting 在 CH30 为 85/90，但 train grouped CV 略低，因此只保留为 secondary，不事后替换主模型。

该结果远高于 zero-shot base VLM，但不是“图像无用”的结论：RF 使用了 831 条 split-stage 监督，base VLM 未做领域训练。公平判断图像增量仍需在相同 train blocks 下比较 supervised numeric-only、images-only 与 combined。RF impurity importance 主要集中于 `n_spikes`（0.601）和 `n_overclusters`（0.241），提示强 numeric shortcut；final-test block 尚未使用。完整分析见 `CH30_NUMERIC_BASELINE_20260921.md`。

### 7.2.3 当前协议下的 OpenAI API-VLM 对照

2026-09-22 使用同一份 `cM2-e008_021-028_CH30`、同一 90 条 fixed-state expert edits 和严格 `action-only-json-v2`，重新评估 GPT-4.1 与 GPT-5.1。两者均通过官方 OpenAI API 接收真实诊断图和数值指标；未使用 mock/RAG，未修改 MAT，也未执行预测动作。

| 模型 | 实际 API 版本 | Accuracy | Macro-F1 | DISCARD recall | SPLIT recall | 严格 JSON |
|---|---|---:|---:|---:|---:|---:|
| GPT-4.1 | `gpt-4.1-2025-04-14` | 35/90 = 0.389 | 0.575 | 0/41 | 29/43 = 0.674 | 90/90 |
| GPT-5.1 | `gpt-5.1-2025-11-13` | 33/90 = 0.367 | 0.564 | 0/41 | 27/43 = 0.628 | 90/90 |

两模型预测一致 88/90（97.8%），都从未输出 `DISCARD`；差异主要不是输出格式，而是与专家动作定义不一致。6/6 `MERGE` 仍是 positive-only，不代表会拒绝错误 merge。GPT-5.1 的一次 512-token 探索运行因 reasoning 用尽预算产生空响应，已停止并排除；正式运行使用 2,048-token ceiling 后 90/90 完整。

该结果与旧 GPT closed-loop cluster-level F1 不矛盾：旧结果衡量执行整条策略后的终态，新结果衡量固定人工状态上的逐动作复现。当前 API 模型低于 Qwen3.5-9B、Gemma 和监督式 numeric RF，因此不应直接将 GPT API 当作 `DISCARD` 的可靠 teacher。完整记录见 `CH30_OPENAI_API_FIXED_STATE_20260922.md`。

### 7.2.4 Prompt 协议差异及其影响

2026-09-22 复核确认，当前 fixed-state 对照与旧 GPT autonomous rollout **不使用同一份输入 prompt**：

- 旧 rollout 使用 `src/agent/context.py` 中的详细领域 prompt，显式定义 neuronal morphology、waveform family、ISI、aggregation complexity 和 merge 判断；当时 Phase 1 实际提供 waveform/ISI/tree 三图。
- 当前 fixed-state 样本来自 finetune 数据导出器的 `gemma4_train_reasoned` profile。它只列出 action space、四个数值和“使用图片”，没有上述领域决策定义；当前评测器再将输出约束为 `action-only-json-v2`。
- Git 历史只能追溯到该 profile 在首个 clean snapshot 中已存在；仓库没有保留“简化输入规则为了提高 Gemma 效果”的设计记录或对照实验。因此它应视为 **SFT 导出 profile 被复用到 fixed-state 评测**，不能声称是已验证的 prompt 改进。

必须区分两种简化：`action-only` 输出有明确理由（大多数 train 样本没有可靠 rationale，且可避免截断/解析失败）；删去输入中的领域规则则没有同等证据。对未 SFT 的 base VLM，输入规则缺失可能导致 KEEP/SPLIT 偏置和 DISCARD recall 降低，但目前没有同模型、同状态、只替换 prompt 的对照，所以不能把准确率下降定量归因于 prompt。

后续应保留当前 `minimal-zero-shot-v1` 作为历史基线，并建立版本化 `legacy-detailed-v1`/`domain-policy-v1`。详细 prompt 不应直接继承旧协议中未校准的 500/5,000 硬删除或“全部 NOT_MERGE 后丢弃”语义；任何 prompt 效果结论必须来自冻结输入/图像/模型/参数后的单变量 ablation。

还存在第二个重要协议差异：旧 rollout 的 waveform overlay 使用未保存 RNG 状态的随机 5,000 条抽样和逐线绘制，当前统一图像合约为 500 条确定性等距抽样。首次 legacy-prompt 复现误用新绘图时，CH30 cluster 211 从历史 `DISCARD` 变为 `SPLIT`；重放历史保存的同 prompt+同三图时，它恢复为 `DISCARD`。这证明图像预处理足以改变动作，但后续完整历史输入重放仍只有 7/10 一致，因此不能把总体差异只归因于图像或 prompt；模型推理本身也有非确定性。

### 7.2.5 旧 GPT-5.1 终态的重评估口径

README 保留的历史 aggregate 为 precision/recall/F1 `0.7653/0.8327/0.7671`。直接对当前保存的四个 `evaluation_report.json` 求均值为 `0.7735/0.8327/0.7718`。差异只来自 CH31：历史 aggregate 记录 20,933 false-positive spikes，而当前 `final_assigns.npy` + 当前 MAT + 当前 evaluator 可复算为 18,065，对应 CH31 precision/F1 `0.6829/0.7251`。

因此两组数字必须分别标记为 **historical reported aggregate** 和 **current-artifact re-evaluation**；后者不是新模型运行。正式写作前应对三个 legacy 方法的所有通道统一重算，保留数据哈希、指标代码版本和 per-channel 结果，不事后选择更高值。

### 7.2.6 Legacy detailed-prompt 复现结果

2026-09-22 完成两层复核。第一层对 CH30 保留的 10 组历史 prompt+PNG 做 byte-identical replay；实际模型仍为 `gpt-5.1-2025-11-13`，严格按旧 1,000-token ceiling 和 parse retry 解释，动作一致 `7/10 = 70%`。不一致为 cluster 1 `DISCARD→SPLIT`、cluster 443 `KEEP→DISCARD`、phase2 455→31 `DISCARD→NOT_MERGE`。这不是 prompt ablation，而是相同历史观察下的模型稳定性审计。

第二层从 CH30 `hierarchy.assigns` 启动 fresh autonomous rollout，使用 artifact 恢复的详细 prompt、旧式未设 seed 的随机 5,000-waveform 绘图、旧 500/4,000/5,000 阈值和当前失败安全语义。首个 pilot 在 Phase 2 达到人为 20-call 成本上限后停止，使用 52,770 total tokens，无终态。

按用户要求将上限提高到 100 后，一次全新运行在 21 次成功 API 调用后自然结束，使用 53,191 total tokens。终态精确匹配历史 CH30：1 个 cluster、50,821 spikes，`P/R/F1=1.0000/0.5578/0.7162`。但新旧决策轨迹明显不同：新运行对 cluster 1 连续三次 `SPLIT`、对 305 先 `SPLIT`，对 353 经历 `KEEP→ABSTAIN`；这些额外分支最后被 `<5,000` 硬阈值清除。因此这是 **legacy controller 终态数值复现**，不是 **VLM 逐步 policy 稳定复现**。

原始仓库初始 commit 的源码定义一套供各通道共用的谨慎版 prompt；CH3/CH20 和 CH31 的 call-suffixed 产物与它逐字节一致。CH30 产物以及 CH31 中无 call suffix 的残留文件则包含更严格的另一版 prompt，说明输出目录混有不同 prompt revision/运行残留。CH30 旧终态可在其严格 artifact 协议下数值重现，但该结果对未校准数量阈值高度敏感，且 VLM action 不稳定。简化 prompt 是否降低准确率仍需未来在相同状态、相同图片、相同模型和相同输出协议下做单变量 A/B。完整记录见 `LEGACY_PROMPT_REPRODUCTION_20260922.md`。

### 7.2.7 Legacy 四通道无总调用上限复跑

2026-09-22 完成 CH3/20/30/31 四个真实通道的 fresh autonomous rollout，但事后审计发现错误地把 CH30 strict artifact prompt 用到了全部通道。总调用上限关闭，但保留 provider 失败即停、单状态最多 3 次解析、`ABSTAIN` 和 checkpoint 安全语义。四条均自然结束，共 95 次 API call、239,670 tokens。该实验只能作为 strict-prompt stress test，不能作为 JianZhi 原始共用 prompt 的四通道复现。

| Channel | Calls | Final units | P/R/F1 | Historical F1 | Exact assignments |
|---|---:|---:|---:|---:|---:|
| CH3 | 9 | 0 | `0/0/0` | 0.7508 | No |
| CH20 | 7 | 0 | `0/0/0` | 0.8951 | No |
| CH30 | 21 | 1 | `1.0000/0.5578/0.7162` | 0.7162 | Yes |
| CH31 | 58 | 2 | `0.7955/0.9173/0.8521` | 0.7251 | No |

该 strict-prompt run 平均 `P/R/F1=0.4489/0.3688/0.3921`；保存历史报告重算均值为 `0.7735/0.8327/0.7718`。由于 prompt revision 不同，两者不能用于判断 JianZhi 原 policy 是否稳定，也不能把 CH3/CH20 清空归因于同协议模型波动。仍可确认的是：老 500/5,000 数量阈值会强烈支配终态。完整更正见 `LEGACY_FULL_ROLLOUT_ALL_CHANNELS_20260922.md`。

### 7.3 Autonomous rollout

Autonomous rollout 从初始 assignments 开始，实际执行模型动作并反复更新状态。步数不固定：一次错误 SPLIT/MERGE/DISCARD 会改变后续所有状态，因此不会必然等于人工的 90 步。

它回答的是模型能否独立完成整条 curation，并获得接近人工终态的结果。只有到这一步才是当前安全新路线的完整闭环评测。

## 8. SFT 在本项目中的作用

Base Qwen/Gemma 有通用视觉和语言能力，但没有专门学习本项目的诊断图、动作语义和实验室规则。SFT 使用 train blocks 中的 973 条人工编辑样本训练 LoRA adapter，学习：

{prompt + diagnostic images + metrics} -> {action JSON}

第一版默认 action-only，不让模型学习不完整或泄漏的 rationale。训练后先与同一 backbone 的 base model 在 CH30 validation 上比较，再将冻结后的最终方案在未用于训练/选模的 final-test block 上评估一次。

SFT 的研究意义是检验项目自己的人工 curation 经验能否被一个可本地运行、可更新权重的 student 吸收，而不是永久依赖闭源 API。

### 8.1 Action-only LoRA 和 rationale 的边界

- **LoRA** 是参数高效微调：冻结大部分 base VLM 权重，只训练小型 adapter，对 1,374 条小数据和有限 GPU 更现实。
- **Action-only** 表示目标只是 `{"action":"SPLIT"}` 这类结构化决策，不要求模型复制一段专家自由文字。
- **Long reasoning/rationale** 是模型对波形、ISI、幅度和树结构给出文字理由。它对人工审查、错误分析和交互有用，但自由文字可能与真实决策机制不一致，不能默认当作可信的 chain-of-thought。
- 当前 241 条人工 rationale 全在 CH30 validation 和 final-test block；973 条默认 train actions 没有人工 rationale。若直接用它们训练 reasoning，就会把验证/测试 recording 泄漏到训练。

因此 action-only 是第一版的数据约束，不是认为 explanation 永远无用。后续可以在 train blocks 补标少量 **structured evidence**（如 waveform-valid、ISI-high、merge-similar），比强迫生成长推理更容易验证。

### 8.2 SFT 是否必须

SFT 不是必须条件，而是当前最直接的研究假设之一。可比较的方向包括：

1. **Zero/few-shot base VLM**：不训练，只用 prompt/少样本；是必要 baseline。
2. **数值指标分类器**：用 logistic regression、random forest 或 gradient boosting；便宜、可解释，可检验 VLM 图像是否真带来增量。
3. **RAG/few-shot memory**：不改权重，检索相似 train 案例；适合 lab-specific adaptation，但需严格防泄漏。
4. **LoRA/SFT 行为克隆**：学人工编辑动作；适合现有正向动作日志，但需补 KEEP/NOT_MERGE。
5. **Preference/DPO**：专家或 teacher 在候选动作中选 chosen/rejected；对 merge pair 排序可能比六类分类更自然。
6. **DAgger/互动式模仿学习**：让 student 自主 rollout，再请专家标注它真正访问的错误状态；可缓解 fixed-state 训练与自主轨迹的分布偏移。
7. **Offline/online RL**：只在 reward、负例、安全回滚和 baseline 成熟后再做；当前还不具备直接上 RL 的条件。

建议的最小发文组合是 **base VLM + numeric baseline + LoRA/SFT + autonomous rollout**；RAG、DPO 和 RL 是后续增量，不是当前闭环的前置条件。

## 9. 评估指标

### 9.1 Action-level

- **Accuracy**：预测 action 与人工 action 完全相同的比例。
- **Precision(action)**：模型预测为该 action 的样本中，真正属于该 action 的比例。
- **Recall(action)**：人工属于该 action 的样本中，被模型正确找回的比例。
- **F1(action)**：precision 和 recall 的调和平均；只有两者都高时才高。
- **Macro-F1**：每种 action 的 F1 等权平均，避免大类别主导结果。
- **Invalid-output / abstention rate**：模型格式失败或拒绝决策的比例。

### 9.2 Cluster-level

- **Cluster**：被认为来自同一个 neuron 的一组 spikes；cluster count 是最终识别的 units 数量。
- **ARI**：比较任意两个 spikes 在模型和人工结果中是否同时被分到一起，并校正随机一致。1 为完全一致，0 约等于随机，负值表示比随机更差；不依赖 cluster ID 数字是否相同。
- **Spike-level precision**：预测 cluster 中真正属于匹配人工 unit 的 spikes 比例；低值常表示混入其他 neuron/噪声。
- **Spike-level recall**：人工 unit 的 spikes 中被匹配预测 cluster 找回的比例；低值常表示丢 spike 或过度拆分。
- **Spike-level F1**：spike-level precision 和 recall 的调和平均。
- **Over-split / redundant units**：一个真实 neuron 被分成多个预测 clusters。
- **Over-merge**：多个真实 neurons 被合并成一个预测 cluster。

当前 src/eval/metrics.py 使用“每个预测 cluster 选择重叠最多的人工 cluster”的项目内部指标，可用于同数据内部比较。外部 benchmark 和 SOTA 声明前，必须加入 SpikeInterface 标准 comparator，固定 spike-time tolerance、unit matching 策略和 over-split/over-merge 分类。

## 10. 测试不同模型的意义

Qwen/Gemma 不是 proposal 预先确定的最终模型，而是当前代码支持的候选 backbone。在同一 prompt、图像和 split 上比较 base models，用于比较：

- 科学图表和多图联合判断能力；
- 结构化 action JSON 的稳定性；
- 不同 action 上的偏置；
- 显存、延迟和成本；
- 相同 SFT 后的可学习性和跨 recording 泛化。

应先用小规模 base inference 选出 1–2 个候选，再做相同预算的 LoRA/SFT；不需要对大量模型全部训练。

## 11. 外部比较与 SOTA 条件

当前内部真实数据可以支持：

- 相同初始 assignments 下的 no-curation / heuristic / API-VLM / base-student / SFT-student 对比；
- action-level 人工一致性；
- post-curation 前后的 ARI、cluster count、precision/recall/F1 改变；
- 数值指标、RAG、SFT 等模块的 ablation。

但不能仅凭它宣称“整体 spike sorting SOTA”，因为本项目主要改进 post-curation，内部数据也不是所有外部方法都运行过的公开 benchmark。

### 11.1 公开 benchmark 对本项目的适用性

| 资源 | 直接适用性 | 能评什么 | 接入工作 |
|---|---|---|---|
| SpikeForest / SpikeInterface ground-truth studies | **适合终态评估，不能直接用于 action SFT** | curation 前后的 spike-time precision/recall/accuracy、well-detected/redundant/overmerged units | 在公开 raw recording 上固定同一 initial sorter；将其输出转成 agent 状态；对当前 `split_last_merge` 补建 hierarchy，或改为 SpikeInterface 通用 split/merge operator |
| AECuration 公开数据 | **部分适合** | event/unit 的 neural-vs-noise DISCARD 能力、跨数据集泛化 | 做独立 DISCARD/noise benchmark；它不提供本项目的有序 SPLIT/MERGE 轨迹 |
| SpikeAgent 的 flexible-probe / Neuropixels 实验 | **方法对比适合，数据是否可直接复用需单独核对** | Good/Noise 一致性、VLM 输入 ablation、人机 curation | 需其单元级标注、原始/预保存 sorter 输出和划分说明；任务主要是 Good/Noise，不能当成本项目全动作真值 |

当前代码的主要接入障碍不是 benchmark “不适合”，而是它们通常提供 raw recording + GT spike trains，不提供这个 MATLAB pipeline 特有的 hierarchy tree 和人工 action log。因此最现实的外部线是：

1. 从 SpikeForest 选与当前单通道/低通道形态接近的 paired、monotrode/tetrode 或 MEArec study；
2. 固定 initial sorter 和参数，保存 uncurated output；
3. 让 agent 只做 post-curation，与 no-curation、指标阈值 curation 和可用的自动 curation baseline 比较；
4. 用 SpikeInterface comparator 对同一 ground truth 评估增量，而不将 action accuracy 与 sorter leaderboard 混为一项。

### 11.2 与相关工作的关系和启发

- **SpikeAgent** 是更宽的 ReAct/CodeAct 工具调用 agent，从环境配置、预处理到 sorter 和 curation 都由 LLM 规划；其 VLM curation 主要做 Good/Noise 和 merge analysis，并用多 reviewer 投票。本项目更窄，但可在“有序专家编辑、显式状态转移、本地 student 训练、无泄漏评测”上建立差异。
- **AECuration** 用合成波形训练 autoencoder，可在 clustering 前后过滤 non-neural events；启发是将其作为无监督 DISCARD baseline，并检验 VLM 在小样本 cluster 上是否真超过波形异常检测。
- **SpikeInterface model-based curation** 展示了“人工标注 sorting + quality metrics -> ML curation”的标准 baseline。它既支持按 quality/template metrics 设阈值，也支持在人工标注数据上训练 Random Forest 等分类器并输出标签与置信度；预训练模型跨数据集迁移仍需重新验证。
- **本项目目前没有使用上述自动 curation 功能**：仓库使用 SpikeInterface 表示 sorting、运行模拟 sorter 和做部分评估/格式衔接，但没有调用 `spikeinterface.curation`、`model_based_label_units` 或 UnitRefine。论文实验应增加一个只读数值指标的 Random Forest/gradient-boosted baseline，并与 base VLM、SFT VLM 使用同一划分比较；否则无法判断诊断图和 VLM 是否带来额外价值。
- **SpikeForest/SpikeInterface comparison** 提供标准 ground-truth matching；启发是论文应将“模仿专家动作”与“最终 spike train 更准”分成两层证据。

对外 SOTA 声明至少需要：

1. 使用 SpikeForest、SpikeInterface ground-truth study 或等价公开 benchmark；
2. 所有方法使用相同 raw recording、preprocessing 和初始 sorter；
3. 使用标准 spike-time matching 和 unit matching 协议；
4. 同时报告 accuracy/precision/recall、well-detected/false-positive/redundant/overmerged units；
5. 在多个 recording、noise/drift/SNR 条件上报告置信区间；
6. 区分初始 sorter 的能力和 agent post-curation 带来的增量。

外部协议参考：

- SpikeInterface, eLife 2020: https://elifesciences.org/articles/61834
- SpikeForest, eLife 2020: https://pmc.ncbi.nlm.nih.gov/articles/PMC7237210/
- SpikeInterface benchmark module: https://spikeinterface.readthedocs.io/en/stable/modules/benchmark.html
- SpikeAgent: https://www.biorxiv.org/content/10.1101/2025.02.11.637754v1
- AECuration: https://pmc.ncbi.nlm.nih.gov/articles/PMC11931169/
- SpikeInterface model-based curation: https://spikeinterface.readthedocs.io/en/latest/tutorials/curation/plot_1_automated_curation.html

## 12. Agent 框架定位与 teacher/student 关系

当前框架可归类为 **multimodal sequential decision agent with a deterministic harness**：

- VLM/student 读取多图和数值观察，产生动作；
- Python harness 负责候选生成、动作验证、状态转移、停止和记录；
- curation 是带确定性工具的长时程决策问题，而不是单张图分类。

它可以做成 teacher/student，但必须区分三种 teacher：

1. **Human teacher**：当前最主要的教师；1,374 条专家动作是监督信号。
2. **API teacher**：GPT/Claude 可为 train-only 候选状态生成负例、反馈或软标签，但不是 ground truth；必须单列 provenance。
3. **Environment/evaluator**：通过人工 terminal assignments 或公开 GT 对终态评分；它是评估器，不必然是生成每步标签的 teacher。

因此现在的真实数据主线是 **human-supervised student policy**，而不是已完成的 API-teacher distillation。模拟 Stage 3 已有 teacher-student feedback 代码，但当时主要用于 mock/集成验证，不能当真实 student 已训好。

### 12.1 API teacher 的实际使用边界

API teacher 指通过 GPT/Claude 等外部 API 调用的强模型：它读取 student 的动作、理由、状态摘要以及 oracle/人工参考动作，再生成纠错反馈或软标签。它不是负责更新环境的 Python harness，也不等于人工 ground truth。当前 trajectory runner 实际给 teacher 传入的是空 `images=[]`，所以现有 teacher 只是 GT-informed 文本 critic，还不是重新独立看图的 VLM reviewer。

- 模拟路线的 `GroundTruthTeacher` 和 trajectory runner 已实现此机制；`setting_002/ch_006` 曾完成两步 GPT-4.1 student + GPT-4.1 teacher 的真实 API smoke，用于验证反馈链路。
- 旧 CH3/20/30/31 MAT 闭环中的 GPT-4.1/GPT-5.1 是直接作决策的 API policy，不是监督本地 student 的 teacher。
- 当前 1,374 条真实训练动作来自人工日志；尚未用 API teacher 大规模补标，也尚未完成本地 student 训练。
- 后续不应把 API teacher 作为主系统或测试真值。它只适合在 **train split** 上做不确定样本反馈、候选负例/软标签和蒸馏对照，且必须记录 provenance；validation/test 仍以人工动作、人工终态或公开 ground truth 为准。

### 12.2 与 SpikeAgent 的重合、差异和风险

如果本项目只做“把 waveform/ISI 图发给 GPT，再让它判断 Good/Noise 或 merge”，就与 SpikeAgent 高度重合，不能作为足够的新颖性。当前并非完全撞题，但差异尚需实验结果证明。

| 维度 | SpikeAgent | 本项目可建立的差异 |
|---|---|---|
| 范围 | LLM 规划环境、预处理、sorter、可视化和 curation 的宽流程 | 聚焦 sorting 后的 sequential post-curation |
| 模型 | 主要使用闭源 API LLM/VLM，零样本、多 reviewer | 计划训练可本地部署、可更新权重的 open VLM student |
| 监督 | Good/Noise、merge 分析和 agent/tool execution | 有序专家 SPLIT/MERGE/DISCARD 轨迹、显式状态转移和终态 assignments |
| 安全 | 多 reviewer/ensemble | ABSTAIN、动作合法性检查、不可逆 DISCARD quarantine/回滚 |
| 评估 | 较宽的任务完成和 curation 比较 | recording-block 防泄漏；action fidelity 与 terminal spike quality 双层评估 |

应优先发展的论文 idea：

1. **候选编辑排序 + STOP**：从当前仅记录实际编辑的正例出发，为可执行 split/merge/discard 候选评分，而不是强迫每个状态做六类分类。
2. **本地可训练的 sequential curation student**：比较 numeric-only、base VLM、LoRA/SFT VLM 和 API policy，证明训练收益与图像增益。
3. **不确定性与安全执行**：低置信 ABSTAIN，DISCARD 先 quarantine；报告 accuracy–coverage 和错误删除率。
4. **DAgger-style 补标**：优先请专家标注 student 自主运行实际访问的错误状态，缓解 teacher-forced 训练和 rollout 的状态分布偏移。
5. **双层真实性**：同时衡量是否复现专家动作，以及最终 spike train/units 是否更接近公开 ground truth，避免把动作模仿等同于科学正确性。

次级方向是 **lab-adaptive memory**：将 recording/lab 风格作为显式 context，用严格隔离的 train-only memory 检验跨 lab 迁移；应排在 base/SFT/autonomous baseline 之后。

### 12.3 双-harness 研究设计（JianZhi 范围澄清）

根据 JianZhi 对原始 scope 的说明，项目应区分两套目的不同的实验 harness。这里的 harness 不只是 prompt，而是 prompt、观察输入、模型角色、反馈/更新机制和评估协议的组合。

**Harness A：skill-rich zero-shot expert**

- 研究问题：给通用 VLM 一套较完整的 spike-curation 领域规则后，不训练能达到什么水平。
- 输入 prompt 显式提供 neuronal morphology、split、merge、ISI 等专家规则；这里的 “skill” 指 prompt 内的领域知识和操作规则。
- 不使用 SFT、在线 teacher feedback 或已学习的 memory；可作为强 prompt-only baseline。
- 在同一 harness 内必须冻结 prompt、图像顺序、指标、输出 schema、模型参数和 controller 语义。

**Harness B：feedback-learning student**

- 研究问题：只给预训练 student 基本任务和动作定义，不预先灌入完整专家规则，它能否通过反馈逐步学会 curation skill。
- “从白纸开始”指没有 spike-curation 专项训练/记忆，不是随机初始化模型权重。
- teacher model 读取 student 输出、状态以及 oracle/人工参考标注，将真实监督转成类似专家的反馈；human/GT annotation 才是监督来源，teacher 不是新的 ground truth。
- skill 的更新可通过 trajectory collection、train-only RAG memory、离线 SFT，未来再扩展 DPO/DAgger/continual learning；当前代码尚未实现成熟的在线逐步更新权重闭环。
- 应报告初始 student、每次 adaptation 后 student 以及 held-out recording 泛化，不能只报告最终一次结果。

两套 harness 可以使用不同 prompt，因为它们回答不同研究问题；正确的冻结单位是“每个 harness 的完整协议”，不是强迫全项目只有一个 prompt。模型间比较、SFT 前后比较和同一 learning curve 内部必须保持该 harness 的 prompt 不变。prompt 更新只能作为新版本和新实验，不能在 validation/final-test 过程中静默改变。

当前 scope 的对应关系是：详细 domain prompt 的 GPT/Qwen/Gemma 评测属于 Harness A；真实人工动作、RAG/SFT 数据与 teacher-student 代码为 Harness B 提供基础，但真实数据 student 训练和反馈迭代尚未完成。numeric-only RF 是两者之外的监督基线。此前暂停模拟路线后，实际工作已偏向真实数据 SFT，因此后续应在实验表中显式恢复 A/B harness 标签。

目前不能把 CH30 strict artifact 与 Harness B 画等号。它仍包含大量领域规则，更可能是 Harness A 的另一 prompt revision；JianZhi 的说明支持“detailed zero-shot 与 minimal student”两类设计，但没有证明 strict/cautious artifact 的具体映射。

## 13. 截至 2026-09-21 的实验状态

| 项目 | 状态 | 可声明内容 |
|---|---|---|
| 旧 API-VLM 真实数据闭环 | 已完成 | 端到端概念可行；只作 preliminary/legacy result |
| 17 MAT 审计 | 已完成 | 16 个可监督，CH5 无标注 |
| 1,374 动作回放和图像导出 | 已完成 | 数据管线和 provenance 有效 |
| recording-block split | 已完成（推定 block） | 可用于当前试验；session 尚待确认 |
| 安全的新真实数据 runner | 已实现并通过测试 | 失败不写成 DISCARD，不隐式回退 mock |
| Base Qwen CH30 3-sample smoke | 已完成 | 真实四图链路与解析通过；1/3 仅作集成观察，不作准确率结论 |
| CH30 90-sample action baseline | 已完成（正式 v2） | accuracy 0.378、macro-F1 0.530；DISCARD recall 0.024，不可进入自主执行 |
| `action-only-json-v2` | 已完成真实 vLLM 验证 | 3/3 smoke 和 90/90 full baseline 严格合规，无截断/解析失败 |
| CH30 open-VLM 横向对照 | 已完成 | 2B/4B/9B/Gemma accuracy 为 0.067/0.378/0.500/0.544；除 4B 命中 1 个外均未识别 DISCARD |
| Numeric-only baseline | 已完成 | train-grouped RF CV 0.904；CH30 84/90、DISCARD recall 0.854；merge 仍是 positive-only |
| Qwen/Gemma LoRA/SFT | 未完成 | 尚无 checkpoint 或微调改善结论 |
| 修复后 open-model autonomous rollout | 未完成 | 尚无当前协议下的闭环结果 |
| 正式 no-RAG/RAG 对照 | 未完成 | 只有集成验证，无效果结论 |
| DPO/RL/continual learning | 未开始 | 当前数据和 reward 不足以支持 |

## 14. 当前主线（高到低）

Runpod H100 环境、CH30 正式 v2 baseline、四模型横向对照和本地 numeric-only baseline 已完成；持久化内容位于 `/workspace` Network Volume，可通过新建 Pod 切换 GPU 而不重传数据和模型缓存。当前下一步为：

1. 以 numeric RF 的 CH30 `0.933` 为监督基线，在相同 train blocks 上设计 images-only 与 images+numeric action-only SFT ablation；不使用 final-test。
2. 补齐或明确派生 KEEP/NOT_MERGE，保留 human/derived/teacher provenance；缺少负例时只能评估 expert edit imitation，不能声称完整自主策略。
3. 以 Qwen3.5-9B 为首个 action-only LoRA/SFT 主候选，Gemma-4-E4B-it 作跨架构复核；以 CH30 validation 选模，final-test block 只在方案冻结后使用一次。
4. 在新安全 runner 上执行 autonomous rollout，报告 action-level、cluster-level、错误 split/merge/discard 和 abstention。
5. 补充 SpikeInterface 标准 comparator，再考虑公开 benchmark 和 SOTA 声明。
6. Baseline/SFT 稳定后才做 API-teacher 增益、固定 train-memory no-RAG/RAG 对照；暂不进入 DPO/RL。
