# Spike-sorting post-curation agent：阶段进展与下一步讨论

**汇报日期：2026-09-21**  
**项目周期：2026-07 至今**

## 1. 先说结论

前期工作已经搭建了一个能运行的多模态 post-curation agent：它读取初始 spike-sorting clusters，生成诊断图和质量指标，再决定是否 `SPLIT`、`DISCARD` 或 `MERGE`，并更新 cluster 状态。旧实验也证明了 GPT API 可以在真实 MAT 数据上完成闭环。

最近的工作不是重新做一个项目，而是把这个 proof of concept 变成一个可训练、可复现、能够严格比较的真实数据研究：

- 审计并整理真实数据和专家动作；
- 修复会导致静默错误或数据泄漏的问题；
- 固定训练、验证和最终测试 recording；
- 在同一协议下测试多个开源 VLM；
- 建立一个仅使用数值质量指标的监督学习基线。

目前最重要的发现是：**在 CH30 的已记录专家编辑动作上，一个很小的 numeric-only Random Forest 已经达到很高的准确率。** 这说明当前任务中存在很强的数值规律，也迫使我们更严格地回答一个问题：诊断图和 VLM 在数值指标之外究竟增加了什么？

这不是项目失去意义，而是研究问题变得更清楚了。

---

## 2. 前期工作与当前工作的关系

### 前期：系统与可行性验证

README 中记录的前期工作主要完成了：

- post-curation 的三阶段 agent/controller；
- cluster 诊断图、数值指标和状态更新；
- `SPLIT / DISCARD / MERGE` 等动作执行；
- GPT-4.1、GPT-5.1 在真实数据上的旧闭环实验；
- 模拟数据 Stage 1–6 的工程连通；
- RAG 的存储、检索和 prompt 注入。

这些工作证明了“真实 MAT → 诊断信息 → 模型决策 → cluster 更新 → 最终结果”能够运行。但旧实验的协议、数据版本和输入格式并未完全冻结，因此更适合作为 preliminary feasibility，而不是最终模型结论。

### 当前：严格实验与本地 student 路线

近期工作的重点是：

- 审计 17 个真实 MAT 数据文件；其中 16 个存在可用监督，CH5 无标注；
- 重建 1,374 条真实专家编辑动作和 5,327 张诊断图；
- 按 recording block 划分 train、CH30 validation 和未使用的 final test；
- 区分 action-level 专家动作与 cluster-level 最终标注；
- 固定 `action-only-json-v2` 输出协议；
- 确保 provider 失败不会回退 mock，解析失败返回 `ABSTAIN` 而不是误执行 `DISCARD`；
- 使用完整 dataset ID，避免不同 recording 中同名 CH 相互覆盖；
- 统一训练与推理时使用的四张诊断图及数值指标。

因此，项目正从“API 模型可以跑起来”转向“本地可训练的 student 是否真正学会专家 curation，并能否安全泛化”。

---

## 3. 当前实验设计

### 数据划分

| 数据部分 | Recording blocks | 动作数 | 用途 |
|---|---|---:|---|
| Train | `cM2-e004_001-003`、`cM2-e004_011-015`、`cM2-e007_012-017` | 973 | 训练与 train-only 交叉验证 |
| Validation | `cM2-e008_021-028` 的 CH30 | 90 | 模型选择和消融 |
| Final test | `cM2-e004_004-006` | 311 | 当前未使用；方案冻结后只评估一次 |

这里的 CH 是 recording 中的电极通道编号，例如 CH30 表示第 30 通道；它不是第 30 个实验，也不是一个独立数据版本。

### 当前评测是什么

当前 CH30 实验是 **fixed-state / teacher-forced action evaluation**：每一步都向模型提供人工轨迹中正确的动作前状态，让模型独立预测专家下一步动作。

它测试的是“模型能否复现当前专家编辑”，尚未测试模型从初始 clusters 独立运行到终态的完整 autonomous rollout。后者更难，因为一次错误动作会改变之后的全部状态。

---

## 4. 当前结果

### 4.1 未微调开源 VLM

四个模型使用相同的 CH30 90 条样本、真实诊断图、数值指标和严格 JSON 输出协议：

| 方法 | 是否接受本项目动作监督 | Accuracy | Macro-F1 | DISCARD recall |
|---|---|---:|---:|---:|
| Qwen3.5-2B | 否 | 0.067 | 0.333 | 0 |
| Qwen3.5-4B | 否 | 0.378 | 0.530 | 0.024 |
| Qwen3.5-9B | 否 | 0.500 | 0.570 | 0 |
| Gemma-4-E4B-it | 否 | 0.544 | 0.578 | 0 |

四个模型都能稳定输出合法 JSON，但几乎无法复现专家的 `DISCARD`。因此失败主要不是格式或程序错误，而是未经领域训练的 base VLM 没有学会当前实验室的 curation 决策规则。

### 4.2 Numeric-only supervised baseline

Random Forest 只使用四项数值特征：

- spike 数量；
- overcluster 数量；
- ISI violation rate；
- amplitude coefficient of variation。

模型使用 train blocks 中的 831 条 `SPLIT/DISCARD` 动作训练；模型选择只使用 train recording 的 grouped cross-validation，没有查看 CH30 或 final test。

| 评测 | Accuracy | Macro-F1 / 说明 |
|---|---:|---|
| Train grouped out-of-fold | 0.904 | macro-F1 0.903 |
| CH30 SPLIT/DISCARD | 78/84 = 0.929 | 真正由 RF 预测的部分 |
| CH30 overall expert edits | 84/90 = 0.933 | 包含 6 个单独处理的 MERGE 正例 |

CH30 上的 6 个错误全部是专家 `DISCARD` 被预测成 `SPLIT`；没有把专家 `SPLIT` 错判成删除。数值模型主要依赖 spike 数量和 overcluster 数量。

需要特别说明：6 个 `MERGE` 并不是模型学出来的。训练集和 CH30 只记录了实际执行的 `MERGE`，没有 `NOT_MERGE` 负例，因此当前只对这些正候选使用 constant `MERGE`。真正学得的核心结果是 `SPLIT/DISCARD` 的 92.9%。

---

## 5. 数值基线为什么会这么强？

目前有三种可能，而且可能同时存在。

### 5.1 专家决策本来就高度依赖这些指标

cluster 大小、overcluster 数、ISI violation 和 amplitude variability 本来就是人工 curation 的重要依据。如果专家在多数常规案例上使用相对稳定的规则，简单模型就可能很好地复现这些动作。

### 5.2 当前评测的任务范围比“完整 curation”窄

当前数据主要记录了专家实际执行的编辑，而不是所有可能候选：

- 缺少可靠的 `KEEP` 状态；
- 缺少 `NOT_MERGE` pair；
- merge 数据全部是正例；
- 每一步都从正确的人工状态开始；
- 尚未让模型承受错误累积并自主决定何时停止。

因此，当前任务更接近“在已被选中需要编辑的候选里区分 SPLIT 和 DISCARD”，并不是“从所有 clusters 中完成完整、可停止的自主 curation”。这个较窄的任务确实可能比最初设想的完整 agent 问题简单。

### 5.3 数据生成或候选选择可能形成 numeric shortcut

`n_spikes` 和 `n_overclusters` 可能不仅描述神经信号，也间接暴露了候选是怎样进入 action log 的。如果人工流程或 hierarchy 构造本身使用了类似阈值，模型可能是在复现数据筛选规则，而不是理解波形形态。

Recording-block split 已经降低了同一 recording 泄漏，但 CH30 仍来自相似实验室流程。只有 final test、其他 recording/session 和公开 benchmark 才能进一步判断这个规律是否稳健。

---

## 6. 这是否意味着项目没有意义？

不意味着，但项目不能再预设“VLM 一定优于简单模型”。更诚实的目标应该是：**找出完成可靠 post-curation 所需的最小信息和最合适的 agent 结构。**

### 如果图像在同等监督下带来提升

这将支持多模态 student 的核心假设：数值指标能处理普通案例，而 waveform、ISI、amplitude 和 hierarchy 图能帮助处理数值上相似但形态不同的困难案例。

### 如果图像只在少数困难案例有用

最合理的系统不是“所有样本都交给大 VLM”，而是一个混合 agent：

1. numeric model 处理高置信度常规状态；
2. VLM 处理模糊或分布外状态；
3. 低置信度时 `ABSTAIN` 并请求人工检查；
4. `DISCARD` 先 quarantine，避免不可逆删除。

这种设计更快、更便宜，也更符合科学数据处理中对安全性的要求。

### 如果图像始终没有增量价值

那也应当诚实接受结果：对于当前数据和动作定义，简单数值模型可能已经足够，强行使用 VLM 反而是过度设计。项目仍可以形成有价值的结论：

- 证明哪些 curation 动作可被简单、可解释的指标自动化；
- 揭示只报告 VLM 成绩而缺少 numeric baseline 会高估多模态方法；
- 将 VLM 限定在数值规则无法覆盖的异常、形态判断或跨实验室适应场景；
- 把重点转向安全停止、负例、置信度、状态分布偏移和最终 cluster quality。

换言之，项目的目标应是做出**正确且可验证的 curation agent**，而不是为了使用 VLM 而使用 VLM。

---

## 7. 当前能声明与不能声明的内容

### 可以声明

- 前期真实数据闭环证明了整体工程路径可行。
- 当前已建立可复现、无明显 recording overlap 的真实动作评测协议。
- 未微调 Qwen/Gemma 无法可靠复现专家 `DISCARD`。
- 在当前 CH30 expert-edit task 上，数值指标与专家 `SPLIT/DISCARD` 高度相关。
- 当前 numeric-only supervised baseline 显著强于 zero-shot base VLM。

### 暂时不能声明

- 不能说 Random Forest 已经完成 spike sorting 或完整 curation。
- 不能说 93.3% 代表 autonomous agent 成功。
- 不能说图像无用，因为 numeric model 接受了 831 条监督，而 VLM 仍是 zero-shot。
- 不能说模型学会了 `MERGE/NOT_MERGE`，因为没有 merge 负例。
- 不能说模型知道何时 `KEEP` 或停止，因为当前日志缺少相应人工标签。
- 不能将当前 action accuracy 与旧 GPT 闭环的 cluster-level F1 直接比较。
- final test 尚未使用，因此还没有最终泛化结果或 SOTA 结论。

---

## 8. 建议的下一步

### 第一优先级：做公平的模态消融

在完全相同的 train blocks、CH30 validation、训练预算和动作目标下比较：

1. numeric-only；
2. images-only；
3. images + numeric。

第一版可对 Qwen3.5-9B 做 action-only LoRA/SFT。这里 SFT 的目的不是追求漂亮数字，而是公平检验经过相同领域监督后，诊断图是否提供额外信息。

### 第二优先级：补齐完整 agent 所需的负例

需要明确或构造具有 provenance 的：

- `KEEP`：不应继续编辑的 cluster；
- `NOT_MERGE`：相似但不应合并的 cluster pair；
- 可选 `ABSTAIN`：信息不足或风险过高的状态。

在这些数据补齐前，项目只能称为 expert edit imitation，不能称为完整 autonomous curation policy。

### 第三优先级：安全 autonomous rollout

Fixed-state 指标达标后，再让模型从初始状态独立执行，并报告：

- 动作准确率和每类 precision/recall/F1；
- 错误 `DISCARD`、错误 merge 和过度 split；
- abstention 与 coverage；
- 最终 assignments 与人工终态之间的 ARI、precision、recall、F1；
- 运行时间和成本。

模型与协议冻结后，final-test block 只使用一次。

### 暂时后置

RAG、API-teacher、DPO、RL 和 continual learning 都可以成为后续扩展，但现在不会解决负例不足和模态价值未验证的问题，因此不应先做。

---

## 9. 建议向教授讨论的关键问题

1. 近期论文问题是否聚焦为：诊断图在强 numeric baseline 之外是否提供增量价值？
2. 是否可以请数据提供者确认或补标少量 `KEEP/NOT_MERGE`，尤其是模型最不确定的案例？
3. 是否同意将最终系统设计为 numeric model、VLM 和 `ABSTAIN` 组成的混合 agent，而不是强制所有状态都由 VLM 判断？
4. final-test 是否继续保持未使用，直到数据、模型和阈值全部冻结？
5. 在内部真实数据之外，是否增加带 ground truth 的公开 benchmark，用于评价最终 cluster quality？

---

## 10. 可直接使用的简短汇报

> The previous work established a working multimodal post-curation pipeline and showed that API-based VLMs could operate on real spike-sorting data. My recent work focused on making the study reproducible and trainable: I audited the real datasets, reconstructed 1,374 expert edits, introduced recording-block splits, fixed the action protocol, and benchmarked open VLMs under the same conditions.
>
> On the CH30 validation recording, zero-shot open VLMs achieved 6.7% to 54.4% action accuracy and largely failed to reproduce expert discard decisions. In contrast, a supervised Random Forest using four numeric quality features achieved 92.9% on the learned split-versus-discard task. This is a strong result, but it does not mean that the full curation problem is solved. The current logs contain expert edits rather than all possible states, lack reliable KEEP and NOT_MERGE negatives, and use fixed expert states instead of autonomous rollouts. The merge examples are also positive-only.
>
> The result changes the main research question. Rather than assuming that a VLM is necessary, I now want to test whether diagnostic images provide incremental value beyond a strong numeric baseline under equal supervision. The next experiment is a controlled numeric-only, image-only, and combined LoRA/SFT comparison. Depending on the result, the final system may be a hybrid agent that handles routine cases numerically, uses a VLM for ambiguous morphology, and abstains or requests review for unsafe decisions.

## 11. 相关记录

- [README.md](README.md)：前期系统、模拟流程、RAG 和旧真实闭环概览。
- [CH30_NUMERIC_BASELINE_20260921.md](CH30_NUMERIC_BASELINE_20260921.md)：numeric-only 实验协议和完整结果。
- [CH30_OPEN_VLM_COMPARISON_20260921.md](CH30_OPEN_VLM_COMPARISON_20260921.md)：四个 open VLM 的同协议比较。
- [项目方法与实验状态.md](项目方法与实验状态.md)：当前方法、数据问题、评测边界和论文路线。
